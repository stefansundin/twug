#ifndef FILL_H_
#define FILL_H_

bool fill(std::string &p_str, unsigned int p_length);
/*
fills out the passed string until it is p_length long then returns true.
if the string is longer than p_length false is returned.
*/

#endif /*FILL_H_*/
